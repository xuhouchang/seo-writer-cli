"""seo-writer: local-first, research-gated, claim-safe SEO content production."""

__version__ = "0.1.0"

# Bumped whenever a deterministic rule (gate thresholds, safety patterns,
# export manifest schema) changes in a way that invalidates prior audits.
RULES_VERSION = "2026-08-14.1"
